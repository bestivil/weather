{
    "version": 2,
    "builds": [
      {
        "src": "src/api/server.ts",
        "use": "@vercel/node"
      },
      {
        "src": "src/**/*.{js,ts,jsx,tsx}",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/server.ts"
      }
    ]
  }
  